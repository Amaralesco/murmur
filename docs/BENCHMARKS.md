# Benchmarks

Every performance claim, with the commit and the conditions it was measured
under. A number without conditions is not a number.

Record for each entry: date, commit SHA, what was measured, the result
(throughput, p99), the hardware and whether it was Docker on macOS or a Linux
box, and the replay multiplier used.

Published figures must come from Linux, not Docker on macOS — the VM layer
distorts disk I/O and makes numbers non-comparable.

Target metric is **throughput per core per euro**, not absolute volume.

---

_No entries yet. First baseline due end of Week 1._
